%div{:id => "#{c.id}hello"}
  #showCardModal.modal.hide.fade{"aria-hidden" => "true", "aria-labelledby" => "showCardModalLabel", :role => "dialog", :tabindex => "-1"}
    .modal-header
      %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", :type => "button"} ×
      %h3#showCardModalLabel View Card
    .modal-body
      %p#notice= notice
      = image_tag c.card.url(:large)
      .modal-footer
        .row
          = link_to 'Edit', edit_card_path(c), :class => 'btn btn-primary btn-block'
        .row
          = link_to 'Destroy', c, :method => :delete, :data => { :confirm => 'Are you sure?' }, :class => 'btn btn-danger btn-block'