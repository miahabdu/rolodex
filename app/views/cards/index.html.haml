%h1 Listing cards

/ %table
/   - @cards.each do |card|
/     %tr
/       / %td= card.card
/       / %td= link_to 'Show', card
/       %td= link_to image_tag(card.card.url(:medium)), card

.row
  .span12
    %ul.thumbnails
      - if @cards
        - @cards.each do |card|
          %li= link_to image_tag(card.card.url(:medium)), card
        

%br

= link_to 'New Card', new_card_path
