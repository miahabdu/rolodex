%h1 Listing cards

%table
  - @cards.each do |card|
    %tr
      / %td= card.card
      / %td= link_to 'Show', card
      %td= link_to image_tag(card.card.url(:medium)), card

%br

= link_to 'New Card', new_card_path
